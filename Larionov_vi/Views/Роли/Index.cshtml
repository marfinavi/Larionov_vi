@model IEnumerable<Larionov_vi.Models.Роли>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.НазваниеРоли)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.НазваниеРоли)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.КодРоли">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.КодРоли">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.КодРоли">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
